# Copia este archivo a .env y ajusta los valores

# Clave secreta para firmar los tokens JWT (auth.js)
JWT_SECRET=naturapp_secret_key

# URI de conexión a MongoDB (server.js usa este valor por defecto si no se define)
MONGODB_URI=mongodb://localhost:27017/naturapp
